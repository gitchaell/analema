This directory previously contained JSON schedule files.
Since the scheduling logic is now configuration-based (fixed daily times), these files are no longer needed.
The directory is kept for potential future use or history.
